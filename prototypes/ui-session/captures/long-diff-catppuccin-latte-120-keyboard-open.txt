
 • Edit(src/search/providers/remote/pagination/preserve-first-occurrence-and-next-cursor-boundary.ts)
   ⎿  Added 3 lines, removed 1 line · collapse
     18   export function paginate(page, previousIds) {
     19     const seen = new Set(previousIds);
     20 -   return {items: page.items, nextCursor: page.cursor};
     20 +   const items = page.items.filter(item => !seen.has(item.id));
     21 +   // Keep the provider cursor, including empty filtered pages.
     22 +   return {items, nextCursor: page.cursor};
     23   }

 Tool output: expanded























────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
保留这段草稿
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
~
0.0%/128k (auto)                                                                                                 preview