---
"@jczhang02/pi-stuff-context": patch
"@jczhang02/pi-stuff-goal": patch
---

Resume an active Goal exactly once when Magic Context intentionally bypasses native Pi compaction, while preserving the native `session_compact` path and overflow retry ownership.
