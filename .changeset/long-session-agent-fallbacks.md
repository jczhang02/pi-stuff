---
"@jczhang02/pi-stuff": patch
"@jczhang02/pi-stuff-agents": patch
"@jczhang02/pi-stuff-btw": patch
"@jczhang02/pi-stuff-codex": patch
"@jczhang02/pi-stuff-context": patch
---

Keep delegated Agents usable in long sessions by selecting native forks only when the complete multilingual launch envelope, retained replacement-prompt context, selected Tool schemas, and child-only reserves fit, otherwise using a bounded frozen reference projection. Give children a deterministic extension surface with a terminal provider-payload guard, require `read` whenever Skills are enabled, retry eligible larger model fallbacks after final payload overflow, and preserve durable diagnostics. Reclaim only terminal-proven stale owned artifacts through claimed, crash-resumable, bounded snapshot traversal with orphan recovery; keep BTW overflow retries on one frozen branch snapshot while reusing captured project memory, fail Context projections open without duplicating history, and honor current Pi Codex header deletion semantics.
