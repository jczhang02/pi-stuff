{
	"name": "@jczhang02/pi-stuff-monorepo",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"packageManager": "bun@1.3.14",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"beads:publish": "bun scripts/publish-beads.ts",
		"check": "bun run format:check && bun run typecheck && bun run test && bun run knip && bun run check:generated && bun run check:public && bun run pack:verify",
		"check:generated": "bun scripts/generate-suite.ts --check",
		"check:public": "bun scripts/check-public-safety.ts",
		"format": "biome check --write .",
		"format:check": "biome check .",
		"knip": "knip --no-progress",
		"pack:verify": "bun scripts/verify-package.ts",
		"release:pack": "bun scripts/pack-release.ts",
		"release:publish": "bun scripts/publish-release.ts",
		"release:version": "changeset version && bun run suite:generate && bun install --ignore-scripts",
		"suite:generate": "bun scripts/generate-suite.ts",
		"test": "bun test --timeout 30000",
		"typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.permissions.json && tsc --noEmit -p tsconfig.permissions-tests.json && tsc --noEmit -p tsconfig.agents.json && tsc --noEmit -p tsconfig.agents-tests.json"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.6",
		"@changesets/cli": "2.31.1",
		"@earendil-works/pi-ai": "0.83.0",
		"@earendil-works/pi-coding-agent": "0.83.0",
		"@earendil-works/pi-tui": "0.83.0",
		"@types/bun": "1.3.14",
		"knip": "6.30.0",
		"typebox": "1.3.7",
		"typescript": "5.9.3"
	},
	"trustedDependencies": [],
	"engines": {
		"bun": "1.3.14"
	}
}
