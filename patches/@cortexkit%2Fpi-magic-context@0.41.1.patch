diff --git a/dist/index-0jgz8yja.js b/dist/index-0jgz8yja.js
index f574d21a61cffa52fce1952d7d4aa0d4700eb70c..2fdf431135ce7b7917ad6752df443d567a044245 100644
--- a/dist/index-0jgz8yja.js
+++ b/dist/index-0jgz8yja.js
@@ -8885,4 +8885,4 @@ import { existsSync as existsSync3, readFileSync as readFileSync2, realpathSync
 import { createRequire } from "node:module";
 import { homedir as homedir3 } from "node:os";
 import { dirname as dirname2, join as join2, resolve } from "node:path";
-import { pathToFileURL } from "node:url";
+import { fileURLToPath, pathToFileURL } from "node:url";
@@ -9009,13 +9009,18 @@ function tokenizerPackageRoots() {
     }
     candidates.push(join2(root, "node_modules", "ai-tokenizer"));
   }
-  let ancestor = process.argv[1] ? dirname2(resolve(process.argv[1])) : cwd;
-  while (true) {
-    candidates.push(join2(ancestor, "node_modules", "ai-tokenizer"));
-    const parent = dirname2(ancestor);
-    if (parent === ancestor)
-      break;
-    ancestor = parent;
+  const starts = [fileURLToPath(import.meta.url), process.argv[1] ?? cwd];
+  for (const start of starts) {
+    let ancestor = dirname2(resolve(start));
+    while (true) {
+      candidates.push(join2(ancestor, "node_modules", "ai-tokenizer"));
+      if (basename(ancestor) === "node_modules")
+        candidates.push(join2(ancestor, "ai-tokenizer"));
+      const parent = dirname2(ancestor);
+      if (parent === ancestor)
+        break;
+      ancestor = parent;
+    }
   }
   return [...new Set(candidates)];
 }
diff --git a/dist/index.js b/dist/index.js
index ab4cf6242ad700372ac05ddef01febaadca945e5..340c1092888b39ccc2c12b70b2c09a2eab5a4083 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -16629,14 +16629,14 @@ function safeStableStringify(value) {
     return "";
   }
 }
-function memoizedContent(kind, content) {
+function memoizedContent(kind, content, knownTokens) {
   const key = `${kind}\x00${content}`;
   const cached = contentMemo.get(key);
   if (cached)
     return cached;
   const measured = {
     hash: fnv1a32(key),
-    tokens: kind === "excluded" ? 0 : estimateTokens(content),
+    tokens: knownTokens ?? (kind === "excluded" ? 0 : estimateTokens(content)),
     keyBytes: key.length * 2 + 32
   };
   contentMemo.set(key, measured);
@@ -16655,8 +16655,8 @@ function memoizedContent(kind, content) {
 function memoizedTokens(kind, content) {
   return memoizedContent(kind, content).tokens;
 }
-function partHash(kind, content) {
-  return memoizedContent(kind, content).hash;
+function partHash(kind, content, knownTokens) {
+  return memoizedContent(kind, content, knownTokens).hash;
 }
 function parseVisibleTag(content, tagsByNumber) {
   const match = content.trimStart().match(TAG_PREFIX);
@@ -16918,7 +16918,7 @@ function finalizeParts(drafts, protectedTags, pendingDropTagNumbers) {
     u += uTokens;
     return {
       key: draft.key,
-      contentHash: partHash(draft.kind, draft.content),
+      contentHash: partHash(draft.kind, draft.content, tokens),
       kind: draft.kind,
       tokens,
       uTokens,
@@ -30623,6 +30623,7 @@ async function src_default(pi) {
     log(`${PREFIX2} in-process subagent child detected; skipping full extension registration`);
     return;
   }
+  await preloadTokenizer();
   const unregisterPiSubagentInitContext = registerPiSubagentInitContext(pi);
   registerPiSubagentInitContextCleanup(pi, unregisterPiSubagentInitContext);
   beginBootQuietPeriod();
