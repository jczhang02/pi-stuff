# Statusline 实施规格

[English](../../statusline-spec.md) · 英文为准.

## Problem Statement

原型仍只显示样例, 正式 footer 尚缺少已选定的紧凑层级和完整 Git 状态, ctx 警示也未关联已确认的 compact 策略.

## Solution

实现真实 Pi 数据驱动的双行 footer, Git 采用有界的事件触发读取. 保留既有第三方状态和全局恢复原生 footer 的开关.

## User Stories

1. 作为 Pi 用户, 我希望默认启用已确认的双行状态栏, 无需额外设置.
2. 作为用户, 我希望尽量完整显示目录, 以确认工作位置.
3. 作为用户, 我希望目录后集中显示 ctx 百分比、容量、连续进度条和缓存命中率, 便于扫读.
4. 作为开发者, 我希望右侧显示分支或 detached 身份和 Git 计数, 便于确认仓库状态.
5. 作为用户, 我希望第二行左侧显示模型和思考强度, 保持稳定位置.
6. 作为扩展用户, 我希望第二行右侧显示现有第三方字段, 以保留扩展反馈.
7. 作为窄屏用户, 我希望按优先级整项隐藏字段, 保持双行且不出现残缺文本.
8. 作为开发者, 我希望看到暂存、修改、未跟踪和冲突文件数, 以了解待处理工作.
9. 作为开发者, 我希望从本地获取领先/落后计数而不自动 fetch, 避免状态栏访问网络.
10. 作为开发者, 我希望看到 rebase/merge 等进行中的操作, 避免误判中间状态.
11. 作为 worktree 用户, 我希望显示当前工作树状态, 不混入其他 checkout.
12. 作为用户, 我希望通过原生分支通知和活动触发刷新, 避免持续轮询.
13. 作为用户, 我希望 Git 失败显示未知而非 clean, 避免误导.
14. 作为仓库外的用户, 我希望隐藏 Git 字段, 避免无关错误占位.
15. 作为用户, 我希望 ctx 沿用 Pi 原生统计并处理 compact, 与宿主会话模型一致.
16. 作为用户, 我希望 compact 后未知用量明确标记, 不把旧值当作当前用量.
17. 作为用户, 我希望 ctx 警示关联生效的 compact 配置, 反映距离压缩的远近.
18. 作为关闭自动 compact 的用户, 我希望仍按窗口容量警示, 保留耗尽风险提示.
19. 作为切换模型的用户, 我希望容量和阈值随新模型更新, 不保留旧限制.
20. 作为用户, 我希望 hit 来自当前分支最近有效响应, 反映最近工作.
21. 作为用户, 我希望区分已知零命中与缺少统计, 避免混淆.
22. 作为扩展用户, 我希望字段保留文本、大小写和颜色并稳定按 key 排序, 无需新协议.
23. 作为用户, 我希望全局开关和 reload 能恢复原生 footer, 便于退出定制.
24. 作为用户, 我希望 Git 语义色跟随主题并保留符号和计数, 不依赖颜色单独表达.
25. 作为用户, 我希望刷新、重载和切换会话时输入流畅且资源被清理, 避免状态工作阻塞或串会话.
26. 作为维护者, 我希望真实宿主测试和独立审查, 将原型落实为可验证的实现.

## Implementation Decisions

- 沿用 `59589c467215ec7394b36c2f60cea27e522316ad` 的双行原型: 第一行左目录/ctx/hit、右 Git; 第二行左模型/思考强度. 其他状态字段放第二行右侧, 让位于必要的 Git 重排.
- 默认启用自定义 statusline. 全局 pi-stuff.json 使用 statusline.enabled, 默认 true, /reload 后生效; false 恢复原生 footer. 首版不新增命令、设置面板、项目覆盖或逐字段配置.
- 复用原生 Git 分支变化通知. 初始化、Pi 工具执行结束、切换会话及 /reload 时补充刷新完整 Git 快照. 不新增固定轮询或递归项目监听. Pi 闲置时, 外部普通文件修改可能延迟到下个触发点才反映在计数中. 不自动 fetch 远端.
- 不在 Git 仓库时隐藏 Git 字段. Git 查询失败或超时时仅保留可信身份, 显示弱色 `git ?`, 隐藏过期计数. 后续成功刷新自动恢复, 不反复通知.
- Git 覆盖分支或 detached HEAD 身份、暂存/修改/未跟踪/冲突文件数、领先/落后提交数, 以及 rebase/merge 等进行中的操作. 窄屏优先保留身份、操作和冲突信息, 可选 ctx 统计退让.
- ctx 百分比仍为已用 token / 模型窗口容量. 自动 compact 开启时, T = 窗口容量 - 当前模型生效的 reserveTokens: 低于 0.9T 使用强调色, 从 0.9T 起使用警示色, 从 T 起使用错误色. 自动 compact 关闭时, 从窗口容量的 80% 起警示、90% 起错误色. 百分比和已用进度条同色, 切换模型或生效配置变化后更新阈值.
- ctx 使用 Pi 原生用量. 未知时按实际容量显示 `ctx ?/272k`, 不显示进度条或警示色, 不替换为零或压缩前旧值.
- hit 表示当前会话分支最近一次有效模型响应的 cacheRead / (input + cacheRead + cacheWrite). 输入量已知但无缓存命中时显示 0%, 无有效统计时隐藏 hit.
- 第三方字段直接读取现有 setStatus 条目, 按 key 排序, 保留大小写和颜色, 在第二行右侧以 `·` 分隔. 窄屏从末尾整项隐藏, 不新增注册协议或逐字段配置.
- Pi 只有一个自定义 footer 插槽. 支持第三方状态字段, 不组合另一套完整 footer; 不反复抢占, 由用户选择一套. 关闭开关恢复原生 footer.
- 全面优先复用原生能力. 通过当前 Pi 版本的原生配置读取器获取持久化 compact 设置, 保留其全局/项目/默认值优先级及该版本支持的模型覆盖. /reload 和切换模型时重读. 不访问私有实时宿主对象, 不承诺 SDK 临时覆盖完全一致.
- 沿用原生配置默认值和错误处理. 无法可靠确定 compact 配置时不猜压力阈值, 已知 ctx 数值以中性色显示. 已知 T 非正表示已达到触发边界, 用量已知时使用错误色. 模型/窗口数据缺失保持未知, 不当作零.
- 保持双行、正常空格及圆点分隔, 原始值大小写和自定义标签小写. 可选字段整项隐藏, 在视口允许范围内优先完整目录/Git身份, 仅当身份本身放不下时截断.
- 不开发新的 goal 或账号用量 provider.

## Testing Decisions

主要验收边界沿用现有真实 Pi 终端 fixture, 加载未替换的包入口, 观察可见文本、颜色、布局与生命周期. 使用临时真实 Git 仓库, 外部 provider 可提供确定数据, 不 mock 自有模块. 聚焦组件测试覆盖 footer 能力、配置边界、阈值和分支 usage/Git 失败, 预期值来自已确认例子而非实现生成的快照. 每片运行针对性测试, 最后运行完整适用测试.

使用隔离配置和确定性 provider 数据验证支持的真实 Pi 宿主 0.85.1、0.86.1、0.87.1. 覆盖正常/detached/尚无提交分支、修改/冲突/进行中的 Git 操作、worktree、查询失败及恢复、外部分支变化. 检查 150/100/80/50 列、明暗主题、compact 后 ctx 未知、两种颜色阈值模式及边界、切换模型、分支/恢复会话的 hit 选择、原生 setStatus 更新/移除、reload 和恢复原生 footer. 确认异步 Git 查询不阻塞输入, 释放时清理资源.

运行适用仓库测试和检查, 完成独立全量 diff 标准/需求审查, 发布真实终端截图和运行证据. 原型样例仅为视觉参考, 不代表生产数据或验收. 除已同意的配置字段外, 本设计不引入新依赖、私有宿主补丁、Pi fork 或生产持久化格式. 合并及发布另行授权.

## Out of Scope

不开发新的 goal/额度 provider、segment 注册 API、完整 footer 混合、固定 Git 轮询、递归项目监听、项目级或逐字段配置、新命令/面板、SDK 临时配置一致性、私有宿主补丁、Pi fork、自动 fetch、合并或发布.

## Further Notes

访谈后维护者已确认. Issue #117 / Beads pi-stuff-wv0, 草稿 PR #118. 原型 #110 / #111 基线59589c467215ec7394b36c2f60cea27e522316ad. 正式分支codex/statusline-implementation起点94707f5, 生产实现前基线51ccadf. 负责人codex:01a0c7d6-a1a0-76d2-9995-08ea5fa365b3. 改变已确认边界需另作决策.
